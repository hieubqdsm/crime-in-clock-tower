---
id: F-002
name: Proximity sound tokens
status: dev_done
branch: feat/F-002-proximity-sound-tokens
priority: P1
assigned: agent
depends_on: [F-001]
auto_test: pass
playtest:
  assigned: tester
  result: pending
  checklist:
    - "4 tokens stand at the room corners, each glowing its own color"
    - "Each token plays a distinct looping sound (tick-tock / chime / hum / drip)"
    - "Sounds are silent from far away, clearer the closer you walk"
    - "The sound clearly comes from the token's corner (3D panning)"
  notes: ""
---

# Proximity sound tokens

## Description
First core mechanic toward the party game: position-based audio. Wooden
tokens stand at the four corners of the room, each emitting a distinct
looping sound through an AudioStreamPlayer3D — audible only when the player
approaches, louder/clearer with proximity (inverse-square attenuation), and
panned in 3D so the direction is readable. This is the seed of the
"walk close to eavesdrop" party mechanic.

## Acceptance criteria (when is it done)
- [x] 4 procedural WAV loops (tick-tock, chime, hum, drip) generated by a
      committed, deterministic script (tools/audio/make_token_sounds.py)
- [x] SoundToken.tscn entity scene: token mesh + glow light + looping
      AudioStreamPlayer3D with distance attenuation
- [x] Main scene places one token per room corner, each with its own sound
- [x] Headless wiring test passes (15/15 — streams, loops, attenuation, corners)
- [x] Web build verified: boots with audio, telemetry shows range semantics
      (spawn: near 12.2 m, aud 0 → approach: near shrinks into range)
- [ ] Tester listens: each corner fades in with proximity, direction readable

## Plan / sub-tasks (cell)
- [x] Feature file + branch + ROADMAP row
- [ ] Generate the 4 WAV loops (stdlib-only Python, seamless loops)
- [ ] SoundToken.tscn + sound_token.gd
- [ ] Wire 4 tokens into Main.tscn (corners, distinct streams)
- [ ] Headless test + web rebuild + browser check

## Automated testing (auto_test)
Headless scene test (planned): `godot --headless --path . res://tests/test_tokens.tscn`
— tokens exist at corner positions, streams assigned, players playing,
loop enabled, attenuation params sane.

## Playtest checklist (for the tester)
- [ ] Click the game once (browser audio needs a click), then walk toward
      each corner and listen: sound fades in as you approach
- [ ] Each corner sounds different; you can tell the direction by ear
- [ ] From the room center, corners are quiet/silent

## Changelog
- 2026-09-10 — (audibility fix, chat feedback "không nghe được / không loop")
  — WAVs normalized to 0.85 peak (were ~0.5), token unit_size 2.0 -> 3.5
  (full volume within 3.5 m), volume_db +2, max_distance 9. Looping made
  bulletproof: 0.5 s watchdog in sound_token.gd restarts playback if the
  stream ever finishes (the importer ignores edit/loop_mode AND the RIFF
  smpl chunk — runtime mutation stays as first line of defense).
  NEW WEB LESSON: AudioStreamPlayer3D.get_playback_position() in a
  single-threaded web build DEADLOCKS the main loop silently (used it in
  telemetry for diagnosis — boot froze with zero console errors; removed).
  Headless 15/15 + 17/17; web boots, corner approach verified (near 4.4 m,
  aud 1 — docs/evidence/f002-web-corner.png).
- 2026-09-10 — dev_done. CRITICAL web lesson: WAV imports default
  compress/mode=2 (Disk streaming) and DISK-STREAMED AUDIO HANGS THE
  SINGLE-THREADED (nothreads) WEB BUILD SILENTLY at scene load (no console
  error — engine boots then freezes). Fix: compress/mode=0 (raw PCM in RAM)
  in every .wav.import; deleting .godot/imported/<file> is required for a
  param change to apply. The importer's edit/loop_mode param is IGNORED in
  4.7 — looping is enabled by a guarded one-time runtime assignment in
  sound_token.gd. Telemetry: near (distance to closest token) + aud (tokens
  within 10 m). Web verified end-to-end (docs/evidence/f002-web-tokens.png).
- 2026-09-10 — feature opened (branch + roadmap row)
