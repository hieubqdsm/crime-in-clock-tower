---
id: F-003
name: Online foundation — websocket server + options UI
status: dev_done
branch: feat/F-003-online-foundation
priority: P1
assigned: agent
depends_on: [F-001, F-002]
auto_test: pass
playtest:
  assigned: tester
  result: pending
  checklist:
    - "Options menu (press O) lets you type a server URL and connect"
    - "Different BROWSERS joining the same server = different mannequins in one room"
    - "Same browser, second tab TAKES OVER the character (old tab stops controlling)"
    - "You see the other player walk/sprint around in real time"
    - "Server runs from this repo: python tools/server/server.py"
  notes: ""
---

# Online foundation — websocket server + options UI

## Description
First multiplayer slice toward the online party game. A tiny Python websocket
server (committed in this repo) keeps a room of players; the game gains an
Options UI with a server-URL field. Player identity persists in the
BROWSER's localStorage, so each browser is its own character, while a second
tab of the same browser takes over that character instead of spawning a
duplicate (server-side takeover by player id).

## Acceptance criteria (when is it done)
- [x] tools/server/server.py: join/world-state protocol, takeover by id, 12 Hz tick
- [x] NetClient (in-game): connect/poll/JSON, own-position upload, remote-state apply
- [x] RemotePlayer: mannequin avatar with interpolation + Walk/Idle + name Label3D
- [x] Options UI (press O): server URL LineEdit + connect/disconnect + status
- [x] Player id persisted in browser localStorage (desktop: user:// file)
- [x] Headless e2e auto-test: 2 clients join one local server, takeover works
- [x] Deployed to Pages; localhost server reachable from the https build

## Plan / sub-tasks (cell)
- [x] Feature file + branch + ROADMAP row
- [ ] Server (python websockets)
- [ ] Identity + NetClient + RemotePlayer
- [x] Options UI
- [x] Headless e2e test (spawns the real server)
- [ ] Web verify (2 tabs, localStorage trick) + deploy

## Automated testing (auto_test)
Planned: `godot --headless --path . res://tests/test_online.tscn`
(spawns tools/server/server.py itself, joins 2 clients, asserts takeover + state).

## Playtest checklist (for the tester)
- [ ] `python tools/server/server.py` in a terminal
- [ ] Open the game (Pages URL or local), press O, connect (default ws://localhost:8765)
- [ ] Open the game in ANOTHER browser, connect — two mannequins, both move
- [ ] Second tab in the SAME browser + connect — your character jumps to that tab's control

## Changelog
- 2026-09-10 — dev_done: verified end to end. Headless e2e 4/4 against a real
  spawned server (join, state round-trip, takeover code 4001). Web: two tabs
  with distinct localStorage ids connect via the real Options UI (O →
  Connect), each renders the other's mannequin (remotes=1), movement syncs;
  same-id second tab takes over (spectator-verified). Gotchas found: hand-
  written .tscn NEEDS explicit [connection] for button signals; webviews may
  resolve localhost as ::1 (default URL is 127.0.0.1); GDScript str(false)
  is "False" — invalid JS in eval; background tabs are rAF-throttled (their
  net poll pauses — players should keep the game tab foreground);
  Pages/browser cache ~10 min can serve a stale pck after redeploy.
- 2026-09-10 — feature opened (branch + roadmap row)
