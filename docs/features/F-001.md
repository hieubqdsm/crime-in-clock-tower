---
id: F-001
name: Wooden mannequin isometric room movement
status: dev_done
branch: feat/F-001-mannequin-isometric-room
priority: P1
assigned: agent
depends_on: []
auto_test: pass
playtest:
  assigned: tester
  result: pending
  checklist:
    - "Mannequin moves with WASD / arrow keys"
    - "Movement direction matches the isometric camera (screen-relative feel)"
    - "Walls block the mannequin, it stays inside the room"
    - "Mannequin visually rotates toward its movement direction"
  notes: ""
---

# Wooden mannequin isometric room movement

## Description
First playable vertical slice of the game: a wooden artist mannequin model
(built in Blender, exported as glb) that the player moves around a closed 3D
room, viewed from a fixed isometric orthographic camera. This establishes the
core presentation (3D isometric) and locomotion every later feature builds on.

## Acceptance criteria (when is it done)
- [x] Godot project exists (`project.godot`), boots headless without errors
- [x] Wooden mannequin 3D model (glb) in the project and used by the player scene
- [x] Player scene: CharacterBody3D + collision, moves with WASD/arrows
- [x] Room scene: floor + 4 walls with collision (closed play space)
- [x] Isometric orthographic camera; movement is camera-relative
- [x] Main scene composes Room + Player + camera + lighting

## Plan / sub-tasks (cell)
- [x] Init repo (project name, project.godot)
- [x] Build mannequin model in Blender headless, export glb into assets/models
- [x] Player.tscn (CharacterBody3D, collision capsule, glb mesh, movement script)
- [x] Room.tscn (floor + walls, StaticBody3D + BoxMesh + collisions)
- [x] Main.tscn (Room + Player + isometric Camera3D + light + environment)
- [x] Headless smoke test all scenes; validate scripts

## Automated testing (auto_test)
Scripted-input scene test (`res://tests/test_movement.tscn`) — 9/9 PASS, exit 0:
- player spawns and settles on the floor
- `move_up` moves up-screen (camera-relative, iso camera)
- speed ≈ 2.2 m/s over 1 s
- model rotates to face its movement direction
- "Walk" (glb Walk_loop) plays while moving, stops at rest pose when idle
- velocity zeroes when idle
- north wall blocks movement exactly at inner face + capsule radius

Command:
```
godot --headless --path . res://tests/test_movement.tscn
```
Plus scene smoke tests (`--quit` on Main/Player/Room): 0 errors each.

### Web + browser smoke test (render + real input verification)
The headless test covers logic only; the web build verifies the actual render
and the input pipeline in a real browser (ZCode in-app browser, via the
browser-use skill / `node_repl`):

```
godot --headless --path . --export-release "Web Smoke Test"   # → build/web/
python -m http.server 8741 --directory build/web
```

The player pushes telemetry via `JavaScriptBridge.eval("window.gameState={...}")`
(web-only, `scenes/player/player.gd::_push_web_state`). Procedure: open
`http://127.0.0.1:8741/index.html`, click the canvas once to give the webview
focus, then dispatch keyboard events **on the canvas element** (Godot's web
input ignores document-level events):

```js
document.querySelector('canvas').dispatchEvent(
  new KeyboardEvent('keydown', {code:'KeyW', key:'w', bubbles:true}))
```

Result (2026-09-09): **5/5 PASS** — W held 1 s moved the mannequin 2.24 m
up-screen (dx=dz=-1.58, 2.2 m/s), `Walk` anim while moving, idle + zero
velocity after release, on floor. Screenshot of the live render verified the
isometric room + mannequin visually: `docs/evidence/f001-web-smoke.png`.

Gotchas discovered (do not relearn):
- `document.hasFocus()` is false until a REAL click hits the canvas (CUA click
  works); Godot drops all keyboard input while unfocused.
- Synthetic `KeyboardEvent` must be dispatched on the **canvas**, not
  document/window (which is where they are merely observed).
- Export target dir must exist before `--export-release` (Godot errors out).

## Playtest checklist (for the tester)
- [ ] Mannequin moves with WASD / arrow keys
- [ ] Direction feels right for an isometric view
- [ ] Walls block movement
- [ ] Model looks like a wooden mannequin (wood material, articulated limbs)

## Changelog
- 2026-09-09 — (web smoke) — Web export preset "Web Smoke Test" (nothreads) + gameState telemetry bridge in player.gd; browser test 5/5 PASS (render + real-input verified, screenshot docs/evidence/f001-web-smoke.png); headless test re-passed 9/9 after bridge
- 2026-09-09 — c57cd97 — mannequin glb (Blender headless, generator committed at tools/blender/make_mannequin.py), Player/Room/Main scenes, isometric ortho camera, movement script; auto-test 9/9 pass (fixed: tscn Transform3D row-major serialization, Input.get_vector y-axis sign, glb clip renamed Walk_loop→Walk by importer)
- 2026-09-09 — 7ee050c — roadmap filled (game description, M0)
- 2026-09-09 — 70c0249 — repo init, project.godot created, feature opened
